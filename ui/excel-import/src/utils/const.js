
// 字段类型
const FieldType = {
  TEXT : '文本框',
  SELECT : '下拉框'
}

// 搜索字段值
const SearchField = {
  VALUE: "搜索条件"
}

// 列表展现
const ListField = {
  VALUE: "列表展现"
}

// 需要忽略的字段
const IgnoreFields = ['SYS_ID']

export  {
  FieldType,
  IgnoreFields,
  SearchField,
  ListField
}